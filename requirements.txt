# --- Web / API ---
fastapi>=0.115,<1.0            # Web API 本体 (要件 §10.1)
uvicorn[standard]>=0.32,<1.0   # ASGI サーバ、0.0.0.0 待受 (§3.2)
jinja2>=3.1,<4.0               # HTML 画面 + 出力ファイルのテンプレート生成 (§10.3 / §20.5)
python-multipart>=0.0.12       # multipart/form-data のファイルアップロード受信

# --- ジョブ管理 ---
redis>=5.2,<6.0               # ジョブ状態・キューのバックエンド (§5.5 / §16)
rq>=2.0,<3.0                  # ジョブキュー、MVP は RQ 優先 (§10.2)

# --- 設定 ---
pydantic-settings>=2.6,<3.0   # 固定値/既定値の一元管理 (§6 / §14)

# --- SVG 解析 ---
defusedxml>=0.7.1             # 安全な XML 解析、外部参照/XXE 回避 (§20.4 / §23.2)

# --- モデル変換 ---
ultralytics>=8.3,<9.0        # YOLO .pt -> ONNX 変換 (§10.4 / F-08)
onnx>=1.17                   # ONNX 出力の依存
